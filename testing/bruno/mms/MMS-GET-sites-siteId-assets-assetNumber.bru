meta {
  name: MMS GET sites/{siteId}/assets/{assetNumber}
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/sites/{{siteId}}/assets/{{assetNumber}}?code={{functionKey}}
  body: none
  auth: none
}

docs {
  GET /sites/{siteId}/assets/{assetNumber}
  Function: GetAssetByNumber
  Source: MmsFunctions.cs

  Route parameters are Bruno variables. Set them in the environment or
  on the request before running.
}
