meta {
  name: SANDBOX POST /admin/reg-location/reject
  type: http
  seq: 41
}

post {
  url: {{baseUrl}}/admin/reg-location/reject
  body: json
  auth: none
}

body:json {
  {}
}

docs {
  POST /admin/reg-location/reject
  Source: SandboxAdminEndpoints.cs

  The body is an empty object: this file records that the endpoint
  exists and how to reach it, not what to send. Supply a payload before
  running, or expect a 400 naming the member that is missing.
}
