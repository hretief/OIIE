meta {
  name: ISBM DELETE sessions/{sessionId}/publication
  type: http
  seq: 14
}

delete {
  url: {{baseUrl}}/sessions/{{sessionId}}/publication?code={{functionKey}}
  body: none
  auth: none
}

docs {
  DELETE /sessions/{sessionId}/publication
  Function: RemovePublication
  Source: ConsumerPublicationFunctions.cs

  Route parameters are Bruno variables. Set them in the environment or
  on the request before running.
}
