meta {
  name: ISBM DELETE sessions/{sessionId}/requests/{requestMessageId}/response
  type: http
  seq: 22
}

delete {
  url: {{baseUrl}}/sessions/{{sessionId}}/requests/{{requestMessageId}}/response?code={{functionKey}}
  body: none
  auth: none
}

docs {
  DELETE /sessions/{sessionId}/requests/{requestMessageId}/response
  Function: RemoveResponse
  Source: ConsumerRequestFunctions.cs

  Route parameters are Bruno variables. Set them in the environment or
  on the request before running.
}
