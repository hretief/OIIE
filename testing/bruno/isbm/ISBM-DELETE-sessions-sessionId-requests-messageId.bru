meta {
  name: ISBM DELETE sessions/{sessionId}/requests/{messageId}
  type: http
  seq: 21
}

delete {
  url: {{baseUrl}}/sessions/{{sessionId}}/requests/{{messageId}}?code={{functionKey}}
  body: none
  auth: none
}

docs {
  DELETE /sessions/{sessionId}/requests/{messageId}
  Function: ExpireRequest
  Source: ConsumerRequestFunctions.cs

  Route parameters are Bruno variables. Set them in the environment or
  on the request before running.
}
