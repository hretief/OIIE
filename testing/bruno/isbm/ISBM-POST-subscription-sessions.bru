meta {
  name: ISBM POST subscription-sessions
  type: http
  seq: 25
}

post {
  url: {{baseUrl}}/subscription-sessions?code={{functionKey}}
  body: json
  auth: none
}

body:json {
  {}
}

docs {
  POST /subscription-sessions
  Function: OpenSubscriptionSession
  Source: ConsumerPublicationFunctions.cs

  The body is an empty object: this file records that the endpoint
  exists and how to reach it, not what to send. Supply a payload before
  running, or expect a 400 naming the member that is missing.
}
