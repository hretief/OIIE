meta {
  name: ISBM POST sessions/{sessionId}/requests/{requestMessageId}/response
  type: http
  seq: 24
}

post {
  url: {{baseUrl}}/sessions/{{sessionId}}/requests/{{requestMessageId}}/response?code={{functionKey}}
  body: json
  auth: none
}

body:json {
  {}
}

docs {
  POST /sessions/{sessionId}/requests/{requestMessageId}/response
  Function: PostResponse
  Source: ProviderRequestFunctions.cs

  Route parameters are Bruno variables. Set them in the environment or
  on the request before running.

  The body is an empty object: this file records that the endpoint
  exists and how to reach it, not what to send. Supply a payload before
  running, or expect a 400 naming the member that is missing.
}
