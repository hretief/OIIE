meta {
  name: ISBM POST security-tokens
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/security-tokens?code={{functionKey}}
  body: json
  auth: none
}

body:json {
  {}
}

docs {
  POST /security-tokens
  Function: AddSecurityToken
  Source: ChannelManagementFunctions.cs

  The body is an empty object: this file records that the endpoint
  exists and how to reach it, not what to send. Supply a payload before
  running, or expect a 400 naming the member that is missing.
}
